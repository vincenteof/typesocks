{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.14.37",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna run bootstrap --hoist"
  }
}
